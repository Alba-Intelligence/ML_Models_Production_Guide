---
updated: 2026-05-12
summary: Generated Docker development image artifact emitted from Nix Terranix definitions.
read_when:
  - You need the canonical Docker development container entrypoint
  - You are validating Docker/Nix boundary implementation status
source_file: ../../Dockerfile
---

# Source summary: Dockerfile

## Role

Defines the generated development container for this repository.

## What it does

- Marked as autogenerated from `nix/terranix/docker-artifacts.nix`.
- Uses `python:3.13-slim` as the base image.
- Installs build essentials, curl, and git for development tooling.
- Installs `uv` and runs `uv sync --frozen` against project dependencies.
- Sets `/workspace` as the working directory.

## Practical implication

The Docker image definition is now generated from Nix source-of-truth rather than hand-maintained line-by-line.
