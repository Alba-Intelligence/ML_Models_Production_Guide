---
updated: 2026-05-11
summary: Synthesized summary of the root README used for newcomer onboarding and first-path navigation.
read_when:
  - You are new to the repository
  - You need the canonical "where to start" path before diving into notebooks/wiki pages
source_file: ../../README.md
---

# Source summary: README.md

## Role

Top-level newcomer onboarding page for GitHub readers.

## What it clarifies

- what the repository is right now (reference + implementation scaffold)
- where to start reading (`docs/wiki/current-state.md`, `target-system`, constraints)
- notebook-first workflow (`nbs/` is source of truth, `ml_deploy/` is exported output)
- canonical task-finalization command (`./scripts/finalize-task.sh`)

## Practical implication

New contributors can orient quickly without reverse-engineering repo intent from scattered files.
